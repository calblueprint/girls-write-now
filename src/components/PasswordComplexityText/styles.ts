import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    passwordComplexity: {
        display: 'flex',
        flexDirection: 'row',
        paddingBottom: 8,
      },
    
      passwordErrorText: {
        marginLeft: 8,
      },
});

export default styles;
